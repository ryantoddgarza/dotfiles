#!/bin/bash

set -e

# Actions made relative to this file's path
cd "$(dirname "${BASH_SOURCE[0]}")"

# Load utils
source ../src/utils

main() {
  # Immediately validate sudo user
  validate_sudo

  # Set macOS defaults
  ../src/defaults/main
}

main
